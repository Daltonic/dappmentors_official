#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running lint-staged..."
npx lint-staged

# Check if lint-staged succeeded
if [ $? -ne 0 ]; then
  echo "Error: lint-staged failed! Fix the linting errors before committing."
  exit 1
fi

echo "Linting successful. Proceeding with commit."